enum UnfocusBehavior
{
  /// Leave the unsubmitted text out of sync with the value.
  nothing,

  /// Restore the unsubmitted text from the current value.
  resetValue,

  /// Save the unsubmitted text to the value.
  saveValue,
}
